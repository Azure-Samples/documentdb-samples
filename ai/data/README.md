# Hotels_Vectors.json

This file was create with the following model:

```
text-embedding-3-small
```
It contains vector embeddings for a dataset of hotel descriptions. Each entry in the JSON file includes the hotel name, description, and its corresponding vector representation generated by the specified embedding model.