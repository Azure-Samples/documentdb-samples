# Azure OpenAI Embedding Configuration
# Enable debug mode for verbose logging (true/false)
DEBUG=false

# Azure OpenAI embedding model name (e.g., text-embedding-3-small, text-embedding-3-small)
AZURE_OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Azure OpenAI API version for embeddings
AZURE_OPENAI_EMBEDDING_API_VERSION=2023-05-15

# Azure OpenAI service endpoint URL
AZURE_OPENAI_EMBEDDING_ENDPOINT=https://your-openai-resource.openai.azure.com/

# Azure OpenAI API key (for connection string authentication)
AZURE_OPENAI_EMBEDDING_KEY=your-azure-openai-api-key-here

# Number of embeddings to process in a single batch (affects API rate limits)
EMBEDDING_SIZE_BATCH=16

# Data File Configuration
# Path to source JSON file without vector embeddings
DATA_FILE_WITHOUT_VECTORS=../data/Hotels.json

# Path to JSON file with generated vector embeddings
DATA_FILE_WITH_VECTORS=../data/Hotels_Vector.json

# Path to JSON file with vector similarity data (optional)
DATA_FILE_WITH_SIMILARITY=../data/Hotels_with_similarity.json

# Path to query file with vectors (optional)
QUERY_FILE_WITH_VECTORS=../data/query_vectors.json

# Directory containing data files
DATA_FOLDER=../data

# Name of the field in documents to generate embeddings for
FIELD_TO_EMBED=Description

# Name of the field where embeddings will be stored
EMBEDDED_FIELD=text-embedding-3-small

# Number of dimensions in the embedding vectors (1536 for text-embedding-3-small)
EMBEDDING_DIMENSIONS=1536

# Number of records to load per batch during data insertion
LOAD_SIZE_BATCH=100

# DocumentDB Configuration

# DocumentDB connection string
MONGO_CONNECTION_STRING=mongodb+srv://username:password@cluster.mongocluster.cosmos.azure.com/?tls=true&authMechanism=SCRAM-SHA-256&retrywrites=false&maxIdleTimeMS=120000

# Name of the DocumentDB cluster
MONGO_CLUSTER_NAME=your-cluster-name

# Example connection string format:
# For password authentication:
# mongodb+srv://username:password@your-cluster.mongocluster.cosmos.azure.com/?tls=true&authMechanism=SCRAM-SHA-256&retrywrites=false&maxIdleTimeMS=120000
#

# Notes:
# 1. Replace all placeholder values with your actual Azure resource information
# 2. For production, use Azure Key Vault or environment variables instead of storing keys in files
# 3. The EMBEDDING_DIMENSIONS must match your chosen embedding model:
#    - text-embedding-3-small: 1536 dimensions
#    - text-embedding-3-small: 1536 dimensions
#    - text-embedding-3-large: 3072 dimensions
# 4. Adjust batch sizes based on your API rate limits and performance requirements
# 5. For passwordless authentication, ensure your Azure identity has appropriate RBAC permissions